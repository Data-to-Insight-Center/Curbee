package org.seadva.registry.database.model.obj.vaRegistry;

import com.google.gson.annotations.Expose;
import org.seadva.registry.database.model.obj.vaRegistry.iface.IAgentRolePK;

import javax.persistence.*;
import java.io.Serializable;


/** 
 * Object mapping for hibernate-handled table: agent_role.
 * @author autogenerated
 */

@Embeddable
public class AgentRolePK implements Cloneable, Serializable, IAgentRolePK {

	/** Serial Version UID. */
	private static final long serialVersionUID = -559002654L;

	

	/** Field mapping. */
	@ManyToOne( cascade = { CascadeType.DETACH }, fetch = FetchType.LAZY )
	@org.hibernate.annotations.Cascade({org.hibernate.annotations.CascadeType.SAVE_UPDATE})
	@Basic( optional = false )
	@JoinColumn(name = "agent_id", nullable = false , insertable = false, updatable = false )
	private Agent agent;

	/** Field mapping. */
    @Expose
	@ManyToOne( cascade = { CascadeType.PERSIST, CascadeType.MERGE }, fetch = FetchType.EAGER )
	@org.hibernate.annotations.Cascade({org.hibernate.annotations.CascadeType.SAVE_UPDATE})
	@Basic( optional = false )
	@JoinColumn(name = "role_type_id", nullable = false , insertable = false, updatable = false )
	private RoleType roleType;

 


 
	/** Return the type of this class. Useful for when dealing with proxies.
	* @return Defining class.
	*/
	@Transient
	public Class<?> getClassType() {
		return AgentRolePK.class;
	}
 

    /**
     * Return the value associated with the column: agent.
	 * @return A Agent object (this.agent)
	 */
	public Agent getAgent() {
		return this.agent;
		
	}
	

  
    /**  
     * Set the value related to the column: agent.
	 * @param agent the agent value you wish to set
	 */
	public void setAgent(final Agent agent) {
		this.agent = agent;
	}

    /**
     * Return the value associated with the column: roleType.
	 * @return A RoleType object (this.roleType)
	 */
	public RoleType getRoleType() {
		return this.roleType;
		
	}
	

  
    /**  
     * Set the value related to the column: roleType.
	 * @param roleType the roleType value you wish to set
	 */
	public void setRoleType(final RoleType roleType) {
		this.roleType = roleType;
	}


   /**
    * Deep copy.
	* @return cloned object
	* @throws CloneNotSupportedException on error
    */
    @Override
    public AgentRolePK clone() throws CloneNotSupportedException {
		
        final AgentRolePK copy = (AgentRolePK)super.clone();

		return copy;
	}
	


	/** Provides toString implementation.
	 * @see Object#toString()
	 * @return String representation of this class.
	 */
	@Override
	public String toString() {
		StringBuffer sb = new StringBuffer();
		
		return sb.toString();		
	}


	/** Equals implementation. 
	 * @see Object#equals(Object)
	 * @param aThat Object to compare with
	 * @return true/false
	 */
	@Override
	public boolean equals(final Object aThat) {
		Object proxyThat = aThat;
		
		if ( this == aThat ) {
			 return true;
		}

		if (aThat == null)  {
			 return false;
		}
		
		final AgentRolePK that;
		try {
			that = (AgentRolePK) proxyThat;
			if ( !(that.getClassType().equals(this.getClassType()))){
				return false;
			}
		} catch (org.hibernate.ObjectNotFoundException e) {
				return false;
		} catch (ClassCastException e) {
				return false;
		}
		
		
		boolean result = true;
		result = result && (((getAgent() == null) && (that.getAgent() == null)) || (getAgent() != null && getAgent().getId().equals(that.getAgent().getId())));
		result = result && (((getRoleType() == null) && (that.getRoleType() == null)) || (getRoleType() != null && getRoleType().getId().equals(that.getRoleType().getId())));	
		return result;
	}
	
	/** Calculate the hashcode.
	 * @see Object#hashCode()
	 * @return a calculated number
	 */
	@Override
	public int hashCode() {
	int hash = 0;
//		hash = hash + getAgent().hashCode();
		hash = hash + getRoleType().hashCode();
	return hash;
	}
	

	
}
